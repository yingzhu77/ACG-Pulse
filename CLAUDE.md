# CLAUDE.md

- Always confirm the latest user request before continuing implementation.
- Preserve Game Pulse v1 scope: public read-only dashboard + private admin panel.
- Do not add user registration in v1.
- Trends/hot search are reserved adapters only; do not make them core v1 behavior.
- Keep ingestion independent from AI analysis. AI failure must not block feed collection.
- Use SourceAdapter and LLMProvider abstractions. Do not hardcode a single source or AI provider.
- Do not commit secrets, cookies, API keys, or private tokens.
- Bilibili v1 should prefer self-hosted RSSHub video routes; dynamic routes may be used only as fallback.
- Do not store Bilibili cookies in the database, frontend, or committed files. If needed, pass cookies only to self-hosted RSSHub through local/server env vars.
- UI should be close in mood to yingzhu.xyz but not copied: dark, translucent, content-dense, lightweight gaming feel.
- Avoid official game logos/characters/assets unless the user explicitly confirms usage.
- Before handoff, run server build, client build, server tests, and report failures clearly.
- Before frontend redesign, read `docs/game-pulse-v1-handoff.md` and `docs/game-pulse-frontend-and-launch-plan.zh.md`.
- Frontend reference assets are under `client/public/game-pulse/reference/`.
- Feed cards should support fetched cover images via `coverUrl`, with source-icon fallback when no cover is available.
- Before final frontend polish, consider `docs/game-pulse-dedup-ranking-plan.zh.md`: same-story deduplication, multi-source jump buttons, low-value notice filtering, and recalibrated importance levels.
- 2026-06-03 current blocker: do not continue visual polish until story merge, multi-source links, importance filtering/facets, real feed/source-status pages, and right/admin navigation clarity are fixed or explicitly deferred.
- Current temporary story aggregation is too loose and can merge unrelated items. Treat this as a correctness bug before adding more UI.
- Right admin rail currently duplicates login/admin actions and has unclear navigation. Hide/simplify it or convert it into real source/status shortcuts before polishing.
